services:
  - type: web
    name: anatomy-auto8-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main4:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /health-auto8
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        generateValue: true