services:
  - type: web
    name: anatomy-8point-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main2:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /health-8point
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        generateValue: true